com:
  scriptcasters:
    ms:
      name: "Email Microservice"

server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: Determination1497!
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create

mailgun:
  username:
  pass:
  baseurl:
  messageurl:

